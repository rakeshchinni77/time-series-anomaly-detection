services:
  anomaly_api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: time-series-anomaly-detection-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
